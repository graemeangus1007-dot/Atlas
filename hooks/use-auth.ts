"use client";

/**
 * Reusable auth hook — AuthProvider context.
 */
export { useAuth, useSession } from "@/lib/auth/auth-provider";
export { useAuthRedirect, useIsAuthenticated } from "@/hooks/use-auth-redirect";
