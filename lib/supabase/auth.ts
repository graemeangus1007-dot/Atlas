/**
 * @deprecated Import from `@/lib/auth` instead.
 * Kept so older imports keep working during the Sprint 14.2 migration.
 */
export {
  signUp,
  signIn,
  signOut,
  requestPasswordReset,
  getSessionUser,
  type AuthCredentials,
} from "@/lib/auth";
